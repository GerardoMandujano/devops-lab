apiVersion: v1
kind: Service
metadata:
  name: ms-demo-service
  namespace: dev

spec:
  selector:
    app: ms-demo

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080

  type: NodePort